<Window
    x:Class="SlideGuide.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="SlideGuide - iRacing Coaching Companion"
    Width="400"
    Height="500">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock
                Text="SlideGuide"
                FontSize="24"
                FontWeight="Bold"/>
            <TextBlock
                Text="iRacing Coaching Companion"
                FontSize="14"
                Opacity="0.7"/>
        </StackPanel>

        <!-- Connection Status -->
        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <TextBlock Text="Telemetry Status:" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <StackPanel Orientation="Horizontal">
                <Ellipse
                    x:Name="StatusIndicator"
                    Width="12"
                    Height="12"
                    Fill="Red"
                    Margin="0,0,8,0"/>
                <TextBlock x:Name="StatusText" Text="Disconnected"/>
            </StackPanel>
        </StackPanel>

        <!-- Overlays List -->
        <StackPanel Grid.Row="2">
            <TextBlock Text="Overlays:" FontWeight="SemiBold" Margin="0,0,0,10"/>

            <CheckBox
                x:Name="BrakeOverlayCheckbox"
                Content="Brake Input"
                Margin="0,0,0,8"/>

            <CheckBox
                x:Name="ThrottleOverlayCheckbox"
                Content="Throttle Input"
                Margin="0,0,0,8"/>
        </StackPanel>

        <!-- Controls -->
        <StackPanel Grid.Row="3" Spacing="8">
            <Button
                x:Name="StartStopButton"
                Content="Start Telemetry"
                HorizontalAlignment="Stretch"
                Click="StartStopButton_Click"/>

            <Button
                Content="Reset Overlay Positions"
                HorizontalAlignment="Stretch"
                Click="ResetPositionsButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
